---
phase: 02-library-and-management-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - templates/index.html
  - docs/SMOKE-TESTS.md
autonomous: true
requirements:
  - LIBR-01
  - LIBR-02
user_setup: []
must_haves:
  truths:
    - The wrapper exposes torrents and file listings with enough diagnostics to distinguish empty upstream state from wrapper failure.
    - The UI presents actionable feedback when library or file retrieval fails or returns no playable items.
  artifacts:
    - app.py
    - templates/index.html
  key_links:
    - app.py preserves the current list/files contract while adding diagnostics that the frontend can consume.
    - templates/index.html renders library/file fetch outcomes clearly on touch-first devices.
---

<objective>
Harden catalog and file-listing behavior so users can tell whether the upstream library is empty, unreachable, or missing playable files.

Purpose: Close the uncertainty around LIBR-01 and LIBR-02 by making listing behavior dependable and understandable.
Output: Improved wrapper diagnostics and clearer library/file states in the UI.
</objective>

<execution_context>
@E:/Projects/torrserver/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/torrserver/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/DEPLOYMENT.md
@docs/SMOKE-TESTS.md
@app.py
@templates/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add structured diagnostics to library and file-list routes</name>
  <files>app.py</files>
  <read_first>app.py, docs/SMOKE-TESTS.md</read_first>
  <action>Refactor the wrapper responses around `/api/torrents` and `/api/files/<torrent_hash>` so the backend can report success, empty-state, and upstream failure distinctly without breaking the core payload shape the UI already uses. Keep the existing list and file-stat data available, but add explicit metadata or sibling fields that indicate whether TorrServer was reachable, whether the library/file list is genuinely empty, and what upstream error occurred when it was not. Avoid changing successful response fields in a way that would break existing rendering.</action>
  <verify>Call `/api/torrents` and `/api/files/<torrent_hash>` in both success and failure-like conditions and confirm the JSON shape includes explicit status metadata.</verify>
  <acceptance_criteria>
    - `/api/torrents` distinguishes empty library from upstream failure.
    - `/api/files/<torrent_hash>` distinguishes no playable files from upstream failure.
  </acceptance_criteria>
  <done>The backend library/file routes expose actionable diagnostics without breaking successful payload consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Surface library and file states clearly in the touch UI</name>
  <files>templates/index.html, docs/SMOKE-TESTS.md</files>
  <read_first>templates/index.html, app.py, docs/SMOKE-TESTS.md</read_first>
  <action>Update the UI so library and per-torrent file loading states present meaningful guidance on iPad/touch devices. If the library is empty, say so. If TorrServer is unreachable, show that explicitly. If a torrent opens but no playable files are available, show a file-level empty/error state rather than falling back to a fake filename. Update `docs/SMOKE-TESTS.md` to cover these new states.</action>
  <verify>Reload the UI at mobile/iPad viewport and confirm the empty/error copy changes based on backend diagnostic state.</verify>
  <acceptance_criteria>
    - The main library empty state is actionable.
    - Opening a torrent with no playable files no longer silently falls back to a dummy filename.
    - Smoke-test guidance covers the new catalog/file states.
  </acceptance_criteria>
  <done>The UI communicates library and file-list outcomes clearly without leaving users guessing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Library route returns explicit status metadata
- [ ] File-list route returns explicit status metadata
- [ ] UI shows distinct copy for empty vs unavailable states
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can understand catalog and file-loading outcomes without checking server logs
</success_criteria>

<output>
After completion, create `.planning/phases/02-library-and-management-hardening/02-01-SUMMARY.md`
</output>

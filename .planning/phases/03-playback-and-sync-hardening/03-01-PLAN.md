---
phase: 03-playback-and-sync-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - templates/index.html
  - docs/SMOKE-TESTS.md
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
user_setup: []
must_haves:
  truths:
    - Playback and download flows expose actionable failure states instead of silent breakage.
    - The touch UI can surface playback errors clearly on iPad-class devices.
  artifacts:
    - app.py
    - templates/index.html
  key_links:
    - stream/download endpoint behavior is reflected in browser-side playback error handling.
---

<objective>
Harden playback and download behavior so stream failures, unsupported files, and download issues are visible and debuggable.

Purpose: Close PLAY-01 and PLAY-02 risk around silent media failures.
Output: Better stream/download diagnostics plus browser-side playback failure UX.
</objective>

<execution_context>
@E:/Projects/torrserver/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/torrserver/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/SMOKE-TESTS.md
@app.py
@templates/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich stream and download endpoint diagnostics</name>
  <files>app.py, docs/SMOKE-TESTS.md</files>
  <read_first>app.py, docs/SMOKE-TESTS.md</read_first>
  <action>Improve `/api/stream/<filename>` and `/api/download/<filename>` so failures return structured, user-meaningful error metadata instead of a bare generic 502 response. Preserve range/header behavior for successful responses while adding enough diagnostics for smoke tests and troubleshooting.</action>
  <verify>Exercise stream/download paths with an invalid hash or filename and confirm the error payload is explicit.</verify>
  <acceptance_criteria>
    - Stream/download failures return structured error information.
    - Smoke-test documentation includes playback/download failure checks.
  </acceptance_criteria>
  <done>Playback-related endpoints reveal failure reasons clearly enough to debug without packet inspection.</done>
</task>

<task type="auto">
  <name>Task 2: Surface playback failure states in the touch UI</name>
  <files>templates/index.html</files>
  <read_first>templates/index.html, app.py</read_first>
  <action>Add browser-side handling for stream load/playback failures so the player does not simply sit broken. Show an actionable message or toast on media errors, and ensure the iPad/touch experience exposes a recovery path instead of silent failure.</action>
  <verify>Trigger a playback failure path and confirm the UI surfaces a visible error state.</verify>
  <acceptance_criteria>
    - Media error states are visible in the UI.
    - iPad/touch users are not left with a silent broken player.
  </acceptance_criteria>
  <done>The browser UI reacts explicitly when playback cannot start or continue.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Stream/download invalid-path probes return explicit error payloads
- [ ] UI shows a visible playback failure state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Playback and download failures are visible and diagnosable
</success_criteria>

<output>
After completion, create `.planning/phases/03-playback-and-sync-hardening/03-01-SUMMARY.md`
</output>

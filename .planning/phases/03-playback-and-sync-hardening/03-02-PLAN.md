---
phase: 03-playback-and-sync-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - app.py
  - positions.json
  - docs/SMOKE-TESTS.md
autonomous: true
requirements:
  - SYNC-01
user_setup: []
must_haves:
  truths:
    - Per-file resume state is written safely and read back consistently.
    - Position persistence failure modes are explicit instead of silently ignored.
  artifacts:
    - app.py
  key_links:
    - backend position persistence logic is validated by smoke-test instructions.
---

<objective>
Harden position persistence so resume state remains consistent and diagnosable under repeated writes.

Purpose: Close SYNC-01 risk around fragile file-backed state.
Output: Safer save/load behavior plus verification guidance for resume state.
</objective>

<execution_context>
@E:/Projects/torrserver/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/torrserver/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/SMOKE-TESTS.md
@app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make positions persistence safer and more explicit</name>
  <files>app.py</files>
  <read_first>app.py, positions.json</read_first>
  <action>Refine load/save position behavior so writes are safer and failures are less silent. This may include atomic writes, stricter validation of incoming position payloads, or clearer read/write error handling. Keep the file-backed approach, but make it more reliable under normal repeated player updates.</action>
  <verify>Run save/read probes against `/api/position/<torrent_hash>` and confirm valid writes round-trip predictably.</verify>
  <acceptance_criteria>
    - Valid position writes persist and read back cleanly.
    - The persistence path is less fragile than the original direct overwrite flow.
  </acceptance_criteria>
  <done>Resume state storage is safer and easier to trust during repeated playback updates.</done>
</task>

<task type="auto">
  <name>Task 2: Document and verify resume-state behavior</name>
  <files>docs/SMOKE-TESTS.md</files>
  <read_first>docs/SMOKE-TESTS.md, app.py</read_first>
  <action>Update the smoke-test playbook so resume-state verification includes both successful save/read cycles and failure-oriented checks when relevant. Keep the instructions concrete and tied to the current API behavior.</action>
  <verify>Read the updated smoke-test section and confirm it covers save/read round-trip checks.</verify>
  <acceptance_criteria>
    - Smoke-test instructions explicitly cover position write/read verification.
  </acceptance_criteria>
  <done>Maintainers have a repeatable way to verify resume-state behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Position write/read round-trip behaves predictably
- [ ] Smoke tests document resume-state verification
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Resume-state behavior is safer and verifiable
</success_criteria>

<output>
After completion, create `.planning/phases/03-playback-and-sync-hardening/03-02-SUMMARY.md`
</output>

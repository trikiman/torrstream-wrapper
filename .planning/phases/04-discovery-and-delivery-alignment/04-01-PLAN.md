---
phase: 04-discovery-and-delivery-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - templates/index.html
  - docs/SMOKE-TESTS.md
autonomous: true
requirements:
  - DISC-01
user_setup: []
must_haves:
  truths:
    - Search behavior distinguishes live results, empty results, provider outage, and cached fallback clearly.
    - Users can still understand search state and act on addable results when available.
  artifacts:
    - app.py
    - templates/index.html
  key_links:
    - search API behavior is reflected in UI states and retry/cached-result behavior.
---

<objective>
Harden discovery/search behavior so the UI remains useful even when the upstream search provider is unstable.

Purpose: Close the remaining discovery gap with explicit provider-state handling and result fallback behavior.
Output: Search retry/status/cached-results behavior plus aligned smoke docs.
</objective>

<execution_context>
@E:/Projects/torrserver/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/torrserver/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/SMOKE-TESTS.md
@app.py
@templates/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add richer search state and fallback semantics</name>
  <files>app.py, templates/index.html</files>
  <read_first>app.py, templates/index.html</read_first>
  <action>Extend the search flow so the API returns enough state for the UI to distinguish empty results from provider outage and the UI can present retry/cached-results behavior. If cached successful results exist for the current query, surface them as stale but usable when the provider is down. Preserve add-from-search behavior for live or cached addable results.</action>
  <verify>Exercise search against a failing provider and confirm the UI shows provider-unavailable state plus cached fallback when available.</verify>
  <acceptance_criteria>
    - Provider outage no longer looks identical to “no results”.
    - Cached search results can still be reviewed when present.
  </acceptance_criteria>
  <done>Discovery/search remains understandable and partly usable under provider instability.</done>
</task>

<task type="auto">
  <name>Task 2: Update smoke tests for discovery-state verification</name>
  <files>docs/SMOKE-TESTS.md</files>
  <read_first>docs/SMOKE-TESTS.md, app.py, templates/index.html</read_first>
  <action>Document the expected search states: live results, empty results, provider outage, and cached fallback. Keep the checks tied to the current API/UI behavior.</action>
  <verify>Read the updated smoke-test section and confirm search-state variants are covered.</verify>
  <acceptance_criteria>
    - Smoke tests explicitly cover search-state verification.
  </acceptance_criteria>
  <done>Discovery-state verification is documented clearly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Search UI distinguishes provider outage from empty results
- [ ] Cached-result behavior is explicit
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Search state is understandable even when the provider is unstable
</success_criteria>

<output>
After completion, create `.planning/phases/04-discovery-and-delivery-alignment/04-01-SUMMARY.md`
</output>

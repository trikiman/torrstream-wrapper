---
phase: 01-brownfield-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - docs/SMOKE-TESTS.md
  - AGENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - QUAL-01
  - DELV-02
user_setup: []
must_haves:
  truths:
    - The repo contains a repeatable smoke-verification path for library, search, sync, and playback-related wrapper behavior.
    - AGENTS.md points future work at the smoke-verification document after changes affecting wrapper behavior.
  artifacts:
    - docs/SMOKE-TESTS.md
    - AGENTS.md
  key_links:
    - docs/SMOKE-TESTS.md references the concrete wrapper endpoints exposed by app.py.
    - AGENTS.md directs maintainers to docs/SMOKE-TESTS.md when making runtime-affecting changes.
---

<objective>
Add a lightweight, repeatable smoke-verification path for the wrapper and wire it into project instructions.

Purpose: Close the QUAL-01 gap and make future brownfield changes safer to validate.
Output: A concrete smoke-test document and Codex-facing instructions that reference it.
</objective>

<execution_context>
@E:/Projects/torrserver/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/torrserver/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/DEPLOYMENT.md
@app.py
@templates/index.html
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a concrete smoke-test playbook for critical wrapper flows</name>
  <files>docs/SMOKE-TESTS.md</files>
  <read_first>app.py, docs/DEPLOYMENT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>Create `docs/SMOKE-TESTS.md` as the repeatable smoke-verification playbook for this repo. Include sections for environment prerequisites, local app startup, wrapper API checks for `/api/torrents`, `/api/files/<torrent_hash>`, `/api/search`, `/api/position/<torrent_hash>`, and manual/browser verification of playback behavior. Be explicit about which checks require a real TorrServer instance and which are safe structural checks. Include exact command examples where practical, and note where a real torrent hash or file path must be substituted.</action>
  <verify>Test-Path docs/SMOKE-TESTS.md; Select-String -Path docs/SMOKE-TESTS.md -Pattern '/api/torrents','/api/files/','/api/search','/api/position/','playback'</verify>
  <acceptance_criteria>
    - `docs/SMOKE-TESTS.md` exists and covers library, search, sync, and playback verification.
    - The document distinguishes automated/shell checks from manual/browser checks.
  </acceptance_criteria>
  <done>A maintainer can follow one document to smoke-test the wrapper after changes.</done>
</task>

<task type="auto">
  <name>Task 2: Reference the smoke-test path from repo instructions and current state</name>
  <files>AGENTS.md, .planning/STATE.md</files>
  <read_first>AGENTS.md, docs/SMOKE-TESTS.md, .planning/STATE.md</read_first>
  <action>Update `AGENTS.md` so the project/workflow guidance tells future maintainers to use `docs/SMOKE-TESTS.md` after changes affecting TorrServer integration, playback, search, or pathing. Keep the generated GSD marker sections intact; if a manual note is added, place it outside the managed markers or regenerate cleanly from source artifacts after updating the underlying planning docs. Update `.planning/STATE.md` blockers/concerns or continuity notes if necessary so the new smoke-test path is visible in current project memory.</action>
  <verify>Select-String -Path AGENTS.md -Pattern 'SMOKE-TESTS.md'; Select-String -Path .planning/STATE.md -Pattern 'SMOKE-TESTS.md','smoke'</verify>
  <acceptance_criteria>
    - `AGENTS.md` references `docs/SMOKE-TESTS.md` for runtime-affecting changes.
    - `.planning/STATE.md` reflects that a smoke-test path exists or is the preferred verification path.
  </acceptance_criteria>
  <done>Project instructions and state make the smoke-test workflow discoverable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Test-Path docs/SMOKE-TESTS.md` returns `True`
- [ ] `Select-String` confirms `docs/SMOKE-TESTS.md` mentions `/api/torrents`, `/api/files/`, `/api/search`, and `/api/position/`
- [ ] `Select-String` confirms `AGENTS.md` references `SMOKE-TESTS.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a concrete smoke-verification path discoverable from project instructions
</success_criteria>

<output>
After completion, create `.planning/phases/01-brownfield-baseline/01-02-SUMMARY.md`
</output>

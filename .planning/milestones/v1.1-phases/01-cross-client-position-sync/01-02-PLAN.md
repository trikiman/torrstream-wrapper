---
phase: 01-cross-client-position-sync
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - static/lampa-sync.js
autonomous: true
requirements:
  - QUAL-02
user_setup: []
must_haves:
  truths:
    - Plugin lifecycle listeners are registered exactly once even if the Lampa-detection loop polls many times.
    - The resume seek bails when the user has navigated away from the torrent it was preparing to seek.
  artifacts:
    - static/lampa-sync.js
  key_links:
    - https://github.com/trikiman/torrstream-wrapper/pull/4
---

<objective>
Harden the Lampa plugin's lifecycle handling so listener registration and resume-seek behavior are correct under reload, polling, and torrent-switch races.

Purpose: Close the Devin Review BUG flagged on PR #3 (BUG_pr-review-job-18e35ca32c9744eb8e8d74b5c2c15442_0001) and a related correctness issue caught in the same code path.
Output: Listener registration moved out of the polling loop, plus a guard in `tryResume()` to bail when `current` no longer matches the parsed torrent.
</objective>

<execution_context>
@.planning/milestones/v1.1-phases/01-cross-client-position-sync/01-01-SUMMARY.md
@static/lampa-sync.js
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Register lifecycle listeners exactly once</name>
  <files>static/lampa-sync.js</files>
  <read_first>static/lampa-sync.js</read_first>
  <action>Move `beforeunload`, `pagehide`, and `visibilitychange` registration out of `attemptInit()` (which can run up to 60 times via the polling loop) into a `registerLifecycleListeners()` helper that runs exactly once at module init. Extract the visibilitychange callback to a named `onVisibilityChange` so the browser can deduplicate it.</action>
  <verify>Open Lampa, watch the console — `[torrstream-sync] hooks installed` appears once and tab-switching triggers exactly one POST per switch.</verify>
  <acceptance_criteria>
    - No duplicate registrations under polling.
    - Single `flush()` per tab-switch.
  </acceptance_criteria>
  <done>Lifecycle listeners are deduplicated and registered exactly once.</done>
</task>

<task type="auto">
  <name>Task 2: Bail resume seek on torrent switch</name>
  <files>static/lampa-sync.js</files>
  <read_first>static/lampa-sync.js</read_first>
  <action>In `tryResume()`'s "wait for `<video>` metadata" `setInterval`, on each tick check `current.hash` and `current.file_index` still match the `parsed` torrent. If not, clear the interval and return without seeking.</action>
  <verify>Start a torrent in Lampa, immediately back out, then start a different torrent. The second torrent does not auto-seek to the first one's saved position.</verify>
  <acceptance_criteria>
    - No stale seek across torrent switches.
  </acceptance_criteria>
  <done>Resume seek is scoped to the torrent it was opened for.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Live plugin at `https://tv.trikiman.shop/static/lampa-sync.js` contains `registerLifecycleListeners()` and `onVisibilityChange` after auto-deploy.
- [ ] Switching tabs once during playback triggers a single POST to `/api/position/<hash>`.
- [ ] Switching torrents during the resume window does not seek the new video to the previous saved position.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Plugin behavior is unchanged for the normal play-then-close flow
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.1-phases/01-cross-client-position-sync/01-02-SUMMARY.md`.
</output>

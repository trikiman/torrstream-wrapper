---
phase: 02-library-and-management-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app.py
  - templates/index.html
  - docs/SMOKE-TESTS.md
autonomous: true
requirements:
  - MGMT-01
  - MGMT-02
user_setup: []
must_haves:
  truths:
    - Adding a torrent via magnet or raw hash yields a clear success or failure result.
    - Removing a torrent clears both TorrServer state and wrapper-side position state predictably.
  artifacts:
    - app.py
    - templates/index.html
  key_links:
    - app.py normalizes add/remove request handling and returns explicit outcomes.
    - templates/index.html presents add/remove success and failure states clearly on touch devices.
---

<objective>
Harden add/remove management flows so the wrapper behaves predictably when users add magnets, paste hashes, or remove library items.

Purpose: Close MGMT-01 and MGMT-02 with reliable mutation behavior and user-visible outcomes.
Output: Safer add/remove request handling and clearer UI feedback.
</objective>

<execution_context>
@E:/Projects/torrserver/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/torrserver/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/SMOKE-TESTS.md
@app.py
@templates/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize and harden add-torrent request handling</name>
  <files>app.py, templates/index.html</files>
  <read_first>app.py, templates/index.html</read_first>
  <action>Improve the add-torrent flow so the backend can accept either a magnet link or a bare hash/path value and turn it into a valid upstream add request when possible. Return explicit structured failure messages when input is invalid or the upstream add fails. Update the modal flow in the UI so it validates input clearly, shows upstream failures honestly, and refreshes the library only after a confirmed success.</action>
  <verify>Exercise the add flow with empty input, a malformed value, and a valid magnet/hash case if available.</verify>
  <acceptance_criteria>
    - Empty or malformed add input does not produce ambiguous failure messages.
    - Successful add path refreshes the library and reports success clearly.
  </acceptance_criteria>
  <done>The add flow handles common user input variants and reports results clearly.</done>
</task>

<task type="auto">
  <name>Task 2: Verify remove flow and local cleanup semantics</name>
  <files>app.py, templates/index.html, docs/SMOKE-TESTS.md</files>
  <read_first>app.py, templates/index.html, positions.json, docs/SMOKE-TESTS.md</read_first>
  <action>Review and harden remove behavior so wrapper-side cleanup matches the user-visible result. Ensure removing a torrent also clears saved position state safely, surfaces upstream failures, and offers a deliberate UI action instead of relying on hidden or unimplemented behavior. Update smoke-test instructions to cover remove verification.</action>
  <verify>Remove a torrent in a controlled test case and confirm associated position data is cleared.</verify>
  <acceptance_criteria>
    - Remove failure is visible to the user.
    - Position cleanup behavior is explicit and verifiable.
  </acceptance_criteria>
  <done>The remove flow is predictable and wrapper-side cleanup is verifiable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Add flow distinguishes invalid input from upstream failure
- [ ] Remove flow surfaces success/failure explicitly
- [ ] Smoke tests document add/remove verification
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Mutation flows are understandable and dependable
</success_criteria>

<output>
After completion, create `.planning/phases/02-library-and-management-hardening/02-02-SUMMARY.md`
</output>

---
phase: 01-brownfield-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DEPLOYMENT.md
  - index.html
  - .planning/PROJECT.md
  - .planning/codebase/STRUCTURE.md
autonomous: true
requirements:
  - DELV-02
user_setup: []
must_haves:
  truths:
    - The repo contains one authoritative deployment/runtime document aligned with the current wrapper code and supported access patterns.
    - The legacy root index file is clearly marked as non-authoritative so future edits target templates/index.html instead.
  artifacts:
    - docs/DEPLOYMENT.md
    - index.html
  key_links:
    - docs/DEPLOYMENT.md references the actual backend routes and asset paths defined by app.py, templates/index.html, static/manifest.json, and static/sw.js.
    - index.html contains an explicit warning that templates/index.html is the served frontend source of truth.
---

<objective>
Create an authoritative deployment/runtime document and remove ambiguity around legacy repo artifacts.

Purpose: Close the DELV-02 gap so the repo documents the real supported runtime and path assumptions.
Output: An authoritative deployment doc plus explicit legacy warnings in duplicate collateral.
</objective>

<execution_context>
@E:/Projects/torrserver/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/torrserver/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@app.py
@templates/index.html
@static/manifest.json
@static/sw.js
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author deployment and runtime source-of-truth documentation</name>
  <files>docs/DEPLOYMENT.md, .planning/PROJECT.md</files>
  <read_first>app.py, templates/index.html, static/manifest.json, static/sw.js, .planning/PROJECT.md, .planning/codebase/STACK.md</read_first>
  <action>Create `docs/DEPLOYMENT.md` as the authoritative runtime/deployment document for this repo. Document the actual Flask entrypoint (`app.py`), served frontend source (`templates/index.html`), current route surfaces (`/`, `/manifest.json`, `/sw.js`, `/api/*`), TorrServer dependency configuration (`TORRSERVER_*` and `JACRED_*` env vars), and the supported deployment topologies. Explicitly distinguish direct-root serving from reverse-proxy `/app/` serving, and call out the current hardcoded `/app/` assumptions in manifest/service-worker/template assets as a deployment constraint. Update `.planning/PROJECT.md` context or constraints only if needed so it points readers to `docs/DEPLOYMENT.md` as the operational source of truth.</action>
  <verify>Test-Path docs/DEPLOYMENT.md; Select-String -Path docs/DEPLOYMENT.md -Pattern 'app.py','templates/index.html','/manifest.json','/sw.js','TORRSERVER_URL','/app/'</verify>
  <acceptance_criteria>
    - `docs/DEPLOYMENT.md` exists and mentions `app.py`, `templates/index.html`, `/manifest.json`, `/sw.js`, and `TORRSERVER_URL`.
    - The document explicitly describes both direct-root and `/app/` deployment considerations.
  </acceptance_criteria>
  <done>Deployment/runtime behavior is documented in one authoritative file that matches the current codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Mark legacy duplicate frontend collateral as non-authoritative</name>
  <files>index.html, .planning/codebase/STRUCTURE.md</files>
  <read_first>index.html, templates/index.html, .planning/codebase/STRUCTURE.md</read_first>
  <action>Add a prominent top-of-file warning comment to the root `index.html` stating that it is a legacy prototype copy and that `templates/index.html` is the served frontend source of truth. Do not alter the served template in this task. Update `.planning/codebase/STRUCTURE.md` if needed so the root `index.html` entry explicitly matches the warning language and future maintainers cannot mistake it for the live UI.</action>
  <verify>Select-String -Path index.html -Pattern 'legacy','templates/index.html','source of truth'; Select-String -Path .planning/codebase/STRUCTURE.md -Pattern 'legacy root HTML prototype'</verify>
  <acceptance_criteria>
    - `index.html` contains an explicit legacy/non-authoritative warning naming `templates/index.html`.
    - `.planning/codebase/STRUCTURE.md` still identifies `index.html` as legacy collateral rather than the served frontend.
  </acceptance_criteria>
  <done>Duplicate frontend collateral is clearly marked so future edits target the correct file.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Test-Path docs/DEPLOYMENT.md` returns `True`
- [ ] `Select-String` confirms `docs/DEPLOYMENT.md` references `app.py`, `templates/index.html`, `/manifest.json`, `/sw.js`, and `/app/`
- [ ] `Select-String` confirms `index.html` includes a legacy/source-of-truth warning
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Repository readers can identify the live runtime/deployment source of truth without guessing
</success_criteria>

<output>
After completion, create `.planning/phases/01-brownfield-baseline/01-01-SUMMARY.md`
</output>

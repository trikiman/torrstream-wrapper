---
phase: 04-discovery-and-delivery-alignment
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - templates/index.html
  - static/manifest.json
  - docs/DEPLOYMENT.md
autonomous: true
requirements:
  - DELV-01
user_setup: []
must_haves:
  truths:
    - The shell is clearly installable and gives iPad/Safari users useful install guidance.
    - Delivery docs reflect the installability behavior actually present in the UI.
  artifacts:
    - templates/index.html
    - static/manifest.json
  key_links:
    - UI install behavior matches the shell/documentation assumptions.
---

<objective>
Improve installability and delivery polish for the browser shell, especially on iPad-class Safari usage.

Purpose: Close DELV-01 with a product-visible install path rather than only manifest/service-worker presence.
Output: Install affordances and aligned delivery documentation.
</objective>

<execution_context>
@E:/Projects/torrserver/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/torrserver/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/DEPLOYMENT.md
@templates/index.html
@static/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add install guidance and install prompt behavior to the shell</name>
  <files>templates/index.html</files>
  <read_first>templates/index.html, static/manifest.json</read_first>
  <action>Add an install affordance to the UI. Use `beforeinstallprompt` where supported, and for iPad/iOS-class browsers provide explicit “Add to Home Screen” guidance instead of a dead button. Keep the UI compact and touch-friendly.</action>
  <verify>Load the shell on desktop and mobile-sized browsers and confirm the install affordance behaves sensibly even when native install prompting is unavailable.</verify>
  <acceptance_criteria>
    - Install affordance exists.
    - iPad/Safari users get explicit install guidance instead of a non-working prompt.
  </acceptance_criteria>
  <done>The app shell communicates a real installation path to users.</done>
</task>

<task type="auto">
  <name>Task 2: Align delivery docs with actual install behavior</name>
  <files>docs/DEPLOYMENT.md</files>
  <read_first>docs/DEPLOYMENT.md, templates/index.html</read_first>
  <action>Update deployment documentation so it reflects the shell’s install behavior, including any iPad/Safari-specific guidance and the current install prerequisites.</action>
  <verify>Read the updated deployment doc and confirm the installability section matches the UI behavior.</verify>
  <acceptance_criteria>
    - Delivery documentation describes install behavior accurately.
  </acceptance_criteria>
  <done>Installability behavior and documentation match.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Install affordance exists in the shell
- [ ] iPad/Safari install guidance is explicit
- [ ] Deployment doc reflects install behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The shell is visibly installable and documented as such
</success_criteria>

<output>
After completion, create `.planning/phases/04-discovery-and-delivery-alignment/04-02-SUMMARY.md`
</output>

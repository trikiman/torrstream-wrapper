---
phase: 04-discovery-and-delivery-alignment
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - scripts/smoke_check.py
  - docs/SMOKE-TESTS.md
  - README.md
  - .planning/STATE.md
autonomous: true
requirements:
  - DELV-01
user_setup: []
must_haves:
  truths:
    - The repo has a runnable smoke-check helper for the current wrapper shell.
    - Docs and state point maintainers at that helper.
  artifacts:
    - scripts/smoke_check.py
    - docs/SMOKE-TESTS.md
  key_links:
    - the smoke helper matches the documented checks and current wrapper endpoints.
---

<objective>
Turn the smoke-verification guidance into a runnable helper and align project docs/state with it.

Purpose: Leave the milestone with a practical repeatable delivery verification path, not only prose.
Output: A scriptable smoke-check helper and updated docs.
</objective>

<execution_context>
@E:/Projects/torrserver/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/torrserver/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/SMOKE-TESTS.md
@README.md
@app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a runnable smoke-check helper</name>
  <files>scripts/smoke_check.py, docs/SMOKE-TESTS.md</files>
  <read_first>docs/SMOKE-TESTS.md, app.py</read_first>
  <action>Create a simple smoke-check script that probes the current wrapper shell endpoints (`/`, `/manifest.json`, `/sw.js`, `/api/status`, and selected API routes) and prints pass/fail output. Keep it lightweight and dependency-minimal.</action>
  <verify>Run the script locally against the running wrapper and confirm it reports meaningful output.</verify>
  <acceptance_criteria>
    - The repo contains a runnable smoke-check helper.
    - The helper reflects the current wrapper endpoint contract.
  </acceptance_criteria>
  <done>The repo has an executable smoke-verification helper instead of docs alone.</done>
</task>

<task type="auto">
  <name>Task 2: Point docs and state at the runnable smoke workflow</name>
  <files>docs/SMOKE-TESTS.md, README.md, .planning/STATE.md</files>
  <read_first>docs/SMOKE-TESTS.md, README.md, .planning/STATE.md, scripts/smoke_check.py</read_first>
  <action>Update the docs and state file so maintainers are pointed at the new smoke-check helper first, with the prose checklist acting as the supporting explanation.</action>
  <verify>Read the updated docs/state and confirm the smoke helper is discoverable.
</verify>
  <acceptance_criteria>
    - README or smoke docs mention the helper prominently.
    - STATE points maintainers at the runnable smoke path.
  </acceptance_criteria>
  <done>The executable verification path is documented and discoverable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] smoke_check.py runs against the local wrapper
- [ ] docs/state point to the helper
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The final delivery verification path is executable and documented
</success_criteria>

<output>
After completion, create `.planning/phases/04-discovery-and-delivery-alignment/04-03-SUMMARY.md`
</output>
